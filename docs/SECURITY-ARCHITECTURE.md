# Security architecture
Public static frontend only. No sensitive-data collection. No external network destinations in HTML/JS. Emergency switch is same-origin. Dashboard is demonstration-only. Any real analytics or user data must be isolated in a separately designed backend with explicit data governance.
